version: "3.9"

services:
  web:
    build: ./web
    container_name: library-web

    ports:
      - "9000:5000"

    networks:
      - library-network

networks:
  library-network:
    external: true